"""
ProMe installer — cross-platform (macOS + Windows).

Entry point: `python3 -m prome_installer` (run from repo root).
Thin shell wrappers live at the repo root: `install.sh` (macOS/Linux)
and `install.bat` (Windows), both of which just invoke this module.
"""

__version__ = "0.1.0"
