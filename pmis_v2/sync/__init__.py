"""30-min sync — content layer that turns tracker segments into work_pages.

See sync/runner.py for the entry point. Does NOT touch the memory tree or
run project matching — those live in Dream (nightly).
"""
