{
  "_note": "Paths are placeholders — install.sh generates the real config. Replace __REPO__ with your repo path.",
  "mcpServers": {
    "pmis-memory": {
      "command": "python3",
      "args": ["__REPO__/pmis_v2/mcp_server.py", "--transport", "stdio"],
      "env": {
        "PYTHONPATH": "__REPO__/pmis_v2"
      }
    }
  }
}
